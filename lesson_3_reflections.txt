When would you want to create a remote repository rather than keeping all your work local?

Whenever I want to share my code with others.


Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?

Because sometimes I want to keep new changes


Describe the differences between forks, clones, and branches. When would you use one instead of another?

fork is like clone but on github.branch are just different. Whenever I want to work On existing project with my colleagues I would use fork. when I want to work on my local Pc i would use clone and branch is a whole differenct think.


What is the benefit of having a copy of the last known state of the remote stored locally?

It could be useful to check new version to the original and stable version which we already have.


How would you collaborate without using Git or GitHub? What would be easier, and what would be harder?

It depends. If we are working next to each other using git is much harder than not using it vice versa gitHub could save a lot of time and effort if used in long distances.


When would you want to make changes in a separate branch rather than directly in master? What benefits does each approach have?

When there is a need to first check the changes with others and then publish it as a main branch